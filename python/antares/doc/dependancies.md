```python
#Speech_module
sudo apt install python3-pip

sudo apt install mosquitto

sudo apt install python3-dev

sudo apt-get install libportaudio2 libportaudiocpp0

sudo apt-get install portaudio19-dev

pip3 install paho-mqtt

pip3 install pyaudio

pip3 install vosk

wget https://alphacephei.com/vosk/models/vosk-model-en-us-aspire-0.2.zip

unzip vosk-model-en-us-aspire-0.2.zip

mv vosk-model-en-us-aspire-0.2 model #Rename the model to destination where script is ran from

```

```python
#Reddit API
pip3 install praw

#Authentication
pip install Flask
pip install telegram
pip install telethon
#re , doesnt require a pip install by default. Maybe in embedded environment?
```